

<main class="login-form" style="margin-top:10%;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">Modification d'une tasklist</div>
                    <div class="card-body">

{{ form_start(form) }}
       <div class="form-group row">
       <div class="col-md-6">
    {{ form_row(form.task_list_name, { 
            label: 'Nom de la tasklist'
        }) }}
</div>
 </div>
               <div class="form-group row">
                <div class="col-md-6">
{{ form_row(form.board, { 
            label: 'Nom du Board'
        }) }}
        </div>
         </div>
         <div class="text-center">
             <button class="btn btn-warning">{{ button_label|default('Sauvegarder') }}</button>
            </div>
{{ form_end(form) }}

                            <div class="text-center">
<br>
    <a class="btn btn-success" href="{{ path('app_task_list_index') }}">Retour à la liste des tasklist</a>
<br><br>
                            </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    </div>

</main>
