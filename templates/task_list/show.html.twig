{% extends 'base.html.twig' %}

{% block title %}TaskList{% endblock %}

{% block body %}

<div class="container" style="margin-top:10%;">
<div class="text-center">
<div class="card" style="width: 79rem;">
  <div class="card-body">
    <h5 class="card-title">[TaskList : {{ task_list.id }}]{{ task_list.taskListName }}</h5>
    <p class="card-text">Board : {{ task_list.board}}</p>
    <a class="btn btn-success" href="{{ path('app_task_list_index') }}" class="card-link">Retour à la liste des tasklist</a><br><br>
    <a class="btn btn-warning" href="{{ path('app_task_list_edit', {'id': task_list.id}) }}" class="card-link">Modifier</a><br><br>
        {{ include('task_list/_delete_form.html.twig') }}
  </div>
</div>
</div>
</div>

{% endblock %}
