<form method="post" action="{{ path('app_board_delete', {'id': board.id}) }}" onsubmit="return confirm('Etes vous sur?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ board.id) }}">
    <button class="btn btn-danger">Supprimer le tableau</button>
</form>
