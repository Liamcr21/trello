{% extends 'base.html.twig' %}

{% block title %}Board{% endblock %}

{% block body %}
    <h1>Board</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ board.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ board.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-info" href="{{ path('app_board_index') }}">Retour a la liste des tableaux</a>

    <a class="btn btn-warning" href="{{ path('app_board_edit', {'id': board.id}) }}">Modifier</a>

    {{ include('board/_delete_form.html.twig') }}
{% endblock %}
